# Setup

`pip install -e .`



Adam's random interp demos. Interesting things:

- Using hooks for activation steering and collection in vLLM
- Using PyTorch hooks for activation collection / steering in HuggingFace (including early stopping)
- Standalone notebook for using `dictionary_learning` SAEs